import { StyleSheet, Text, View, ScrollView, TouchableOpacity, Animated } from 'react-native';

export default function CouponInfo({ navigation }) {
    return(
        <View>
            <Text>Coupon Info</Text>
        </View>
    )
}